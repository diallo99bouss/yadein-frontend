import { BrowserModule } from '@angular/platform-browser';
import { NgModule } from '@angular/core';
import { AppRoutingModule } from './app.routing'; // Assurez-vous d'importer le module de routage
import { AppComponent } from './app.component';

@NgModule({
  declarations: [
    
  ],
  imports: [
    BrowserModule,
    AppRoutingModule // Importez et ajoutez le module de routage ici
  ],
  providers: [],
})
export class AppModule { }
